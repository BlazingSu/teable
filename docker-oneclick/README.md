# One Click Docker Deployment

This directory contains a simple setup for running **Teable** with Docker.

## Quick Start

```sh
./deploy.sh
```

The script uses `docker compose` to start Teable and a Postgres database.
All passwords are set to `123456` in the provided `.env` file.

